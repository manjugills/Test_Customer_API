using Xunit;

namespace CustomerService.Application.WebApi.Test
{
    public class UnitTest1
    {
        [Fact]
        public void Test1()
        {
            //Add .net core  hosting and read basic configuarion
            // Add HttpClient on set up
            //Call the exposed urls
        }
        //[Fact]
        //public async Task Get_Customer_Should_Return_Ok()
        //{
        //    // Arrange
        //    // Act
        //    var response = await Client.SendAsync(new HttpRequestMessage(HttpMethod.Options, Endpoint));

        //    // Assert
        //    response.StatusCode.Should().Be(StatusCodes.Status200OK);
        //}
    }
}
